# 20: Дії над Записом

**What to build:** Адміністратор платформи скасовує Запис із причиною, переносить на інший вільний слот або змінює статус. Клієнт дізнається про це так само, як від Салону.

**Blocked by:** 19 (Записи в картці: список і деталі); 06 (Редагування профілю Салону + Журнал дій)

**Status:** ready-for-agent

Специфікація: `../spec.md` · Словник: `CONTEXT.md` · ADR: `docs/adr/`. Тикет зачіпає обидва репозиторії (`bookme-backend` → `admin-api`, `beauty_admin` → UI), якщо не сказано інше.

- [ ] `PATCH /admin/appointments/{id}` (статус: скасовано / завершено / не з'явився), `PATCH …/reschedule`, `GET …/available-slots` — ті самі сервіси, що для бізнесу
- [ ] Скасування вимагає причини; слот-lock звільняється
- [ ] Перенесення — лише на слот, який сервіс доступності вважає вільним
- [ ] Клієнт отримує звичайні сповіщення про скасування й перенесення
- [ ] Скасування дозволене й над Записами Видаленого профілю
- [ ] Кожна дія — рядок Журналу дій
- [ ] Jest: кожна дія, зайнятий слот, заборонений перехід статусу; Playwright
