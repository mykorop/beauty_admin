# 20: Дії над Записом

**What to build:** Адміністратор платформи скасовує Запис із причиною, переносить на інший вільний слот або змінює статус. Клієнт дізнається про це так само, як від Салону.

**Blocked by:** 19 (Записи в картці: список і деталі); 06 (Редагування профілю Салону + Журнал дій)

**Status:** ready-for-human

Специфікація: `../spec.md` · Словник: `CONTEXT.md` · ADR: `docs/adr/`. Тикет зачіпає обидва репозиторії (`bookme-backend` → `admin-api`, `beauty_admin` → UI), якщо не сказано інше.

- [x] `PATCH /admin/appointments/{id}` (статус: скасовано / завершено / не з'явився), `PATCH …/reschedule`, `GET …/available-slots` — ті самі сервіси, що для бізнесу
- [x] Скасування вимагає причини; слот-lock звільняється
- [x] Перенесення — лише на слот, який сервіс доступності вважає вільним
- [x] Клієнт отримує звичайні сповіщення про скасування й перенесення
- [x] Скасування дозволене й над Записами Видаленого профілю
- [x] Кожна дія — рядок Журналу дій
- [x] Jest: кожна дія, зайнятий слот, заборонений перехід статусу; Playwright
