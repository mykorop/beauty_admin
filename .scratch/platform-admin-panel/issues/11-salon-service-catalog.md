# 11: Каталог послуг Салону

**What to build:** Адміністратор платформи бачить Каталог послуг Салону, створює, редагує й деактивує послуги.

**Blocked by:** 06 (Редагування профілю Салону + Журнал дій)

**Status:** ready-for-agent

Специфікація: `../spec.md` · Словник: `CONTEXT.md` · ADR: `docs/adr/`. Тикет зачіпає обидва репозиторії (`bookme-backend` → `admin-api`, `beauty_admin` → UI), якщо не сказано інше.

- [ ] `GET/POST /admin/salons/{salonId}/services`, `GET/PATCH/DELETE …/services/{serviceId}` — ті самі схеми й сервіси
- [ ] Таблиця: назва, Категорія послуги, тривалість, ціна, валюта, активність, кількість Майстрів із Копією цієї послуги
- [ ] Нова послуга — лише в MDL
- [ ] При зміні ціни чи тривалості видно явне попередження: Копії майстрів не змінюються
- [ ] Категорії послуг і спеціалізації доступні в `admin-api` як довідники лише для читання, з перекладами uk / ru / ro
- [ ] Усі зміни — у Журнал дій
- [ ] Jest, зокрема перевірка відсутності каскаду; Playwright на CRUD і попередження
