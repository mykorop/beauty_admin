# 26: Детальна статистика: рушій + ріст, Записи, Вартість Записів

**What to build:** Адміністратор платформи натискає «Порахувати», бачить перебіг і отримує графіки росту, Записів і Вартості Записів із живих даних (ADR-0003).

**Blocked by:** 25 (Дашборд: Базові показники)

**Status:** ready-for-human

Специфікація: `../spec.md` · Словник: `CONTEXT.md` · ADR: `docs/adr/`. Тикет зачіпає обидва репозиторії (`bookme-backend` → `admin-api`, `beauty_admin` → UI), якщо не сказано інше.

- [x] `POST /admin/stats/detailed` запускає обчислення асинхронно й повертає його ідентифікатор; `GET` повертає стан і останній результат із часом побудови; одночасно — не більше одного обчислення
- [x] Обчислення сканує таблицю посторінково й агрегує в пам'яті; результат зберігається одним рядком конфігурації до наступного запуску
- [x] Агрегація — чистий модуль (потік рядків + «зараз» + часовий пояс → статистика), який не знає про DynamoDB
- [x] Ріст: нові Салони, Майстри, Клієнти по днях і тижнях
- [x] Записи по днях за статусами, частка скасувань; минулі «заброньовано» — окрема категорія
- [x] Вартість Записів у MDL за поточним курсом наявного сервісу курсів: без скасованих і «не з'явився»; окремо минула й заброньована наперед; підпис у UI, що це не виручка
- [x] День — за `Europe/Chisinau`
- [x] Юніт-тести модуля: межі дня, перехід на літній час, змішані валюти, виключення статусів, порожня таблиця
- [x] UI: кнопка запуску, перебіг, час останнього результату, три графіки; Playwright на запуск і опитування
